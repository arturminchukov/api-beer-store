const beerValidationSchemes = require('./beerValidationSchemes');
const userValidationSchemes = require('./userValidationSchemes');

module.exports = {
    beerValidationSchemes,
    userValidationSchemes
};
